# BK21 RAG 챗봇 의존성 — 직접 import하는 패키지만 명시
# 실제 의존성 트리는 pip가 해결. 버전 핀이 필요하면 `pip freeze > requirements.lock` 사용.

# 크롤러
requests
beautifulsoup4
python-dateutil

# 데이터 처리
pandas

# RAG 코어
chromadb
openai
python-dotenv

# 규정·매뉴얼 적재
python-docx        # DOCX (mgmt/budget/internal)
pymupdf            # PDF (실무자 매뉴얼)

# 하이브리드 검색
bm25s

# Streamlit UI
streamlit

# 평가 / 토큰 분석
PyYAML             # eval/golden_set.yaml
tiktoken           # estimate_tokens.py
